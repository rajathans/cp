#include <stdio.h>

int main() 
{
int wd;
float bal;
scanf("%d",&wd);
scanf("%f",&bal);
if((wd%5)==0) && (bal-0.5)>=wd)))
{
bal = bal - wd - 0.5;
printf("%2f",bal);
}
else
{
printf("%2f", bal);
}

return 0;
}